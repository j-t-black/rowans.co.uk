---
phase: 03-content-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [app/components/BurgerNav.vue, app/pages/index.vue, app/pages/radio.vue]
autonomous: false
---

<objective>
## Goal
Fix 4 visual regressions from Phase 3 polish work and establish Figma reference screenshot library for ongoing verification.

## Purpose
Regressions from the first polish pass broke user-added features and visual layout. Fixing these and capturing reference screenshots prevents the same issues recurring in future work.

## Output
- 4 bugs fixed (hero opacity, eats overlap, radio buttons, burger nav)
- Figma reference screenshots saved to `design-references/desktop-1920/`
- No regressions on previously polished pages
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Design Doc
@docs/plans/2026-03-06-fix-regressions-improve-workflow-design.md

## Source Files
@app/components/BurgerNav.vue
@app/pages/index.vue
@app/pages/radio.vue
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| Figma MCP (get_screenshot) | required | Capturing reference screenshots | - |
| Figma MCP (get_design_context) | required | Getting exact values for fixes | - |

**Note:** Sub-agents handle their own screenshot capture via Playwright. Orchestrator captures Figma references via MCP.
</skills>

<acceptance_criteria>

## AC-1: BurgerNav Fully Visible at 1920px
```gherkin
Given the burger nav is open at 1920x1080 viewport
When the user looks at the nav list
Then all 14 items including "Home" are visible and scrollable
```

## AC-2: Hero Logo Opacity Matches Figma
```gherkin
Given the home page hero section rendered at 1920px
When compared with the Figma Desktop frame
Then the logo opacity matches the Figma reference
```

## AC-3: Eats+Drinks Heading Properly Spaced
```gherkin
Given the home page Eats+Drinks section at 1920px
When the heading and subtitle are displayed
Then the heading does not overlap the subtitle text and spacing matches Figma
```

## AC-4: Radio Buttons Have Proper Bottom Padding
```gherkin
Given the radio page at 1920x1080 viewport
When the area buttons (Upstairs/Downstairs/Garden) are displayed
Then the buttons have visible padding from the bottom edge of the viewport
```

## AC-5: Figma Reference Library Created
```gherkin
Given the design-references/desktop-1920/ directory
When the user browses it
Then there is a PNG screenshot for each page that has a Figma Desktop frame
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Capture Figma reference screenshots for all pages</name>
  <files>design-references/desktop-1920/*.png</files>
  <action>
    Use sub-agent to capture Figma MCP get_screenshot for each page's Desktop frame:
    - /home (node 2:3)
    - /whats-on (node 12:1158)
    - /radio (node 12:1185)
    - /policies (node 8:948)
    - /groups (node 4:622)
    - /kids (node 8:791)
    - /corporate (node 8:890)
    - /launch (node 4:718)
    - /birthday-cakes (node 8:1021)
    - /drinks-menu (node 8:1042)
    - /merch (node 8:936)

    Save each as design-references/desktop-1920/{page-name}.png
  </action>
  <verify>All 11 PNG files exist in design-references/desktop-1920/</verify>
  <done>AC-5 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Fix BurgerNav clipping at 1920px</name>
  <files>app/components/BurgerNav.vue</files>
  <action>
    Fix the CSS centering issue that clips top items when content overflows viewport.
    The overlay uses justify-content: center which clips overflow top.

    Fix: Replace justify-content: center with a safe centering approach.
    Options: margin: auto on nav-list, or align-content: safe center, or
    remove justify-content and use padding/margin for centering that allows scroll.

    Ensure all 14 items are visible and scrollable at 1920x1080.
    Do NOT change mobile or tablet styles.
  </action>
  <verify>Open burger nav at 1920x1080 — "Home" link visible, all items scrollable</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Fix home page regressions (sub-agent)</name>
  <files>app/pages/index.vue</files>
  <action>
    Dispatch sub-agent to fix two issues in index.vue 1920px media query:

    1. Hero logo opacity: Use Figma MCP get_design_context on node 2:3 to get exact
       opacity value for the logo. Apply correct value.

    2. Eats+Drinks overlap: The eats-heading at 96px has no margin-bottom in the 1920px
       block, causing it to overlap eats-text. Check Figma for correct heading size and
       spacing. Add appropriate margin-bottom to separate heading from subtitle.

    Rules:
    - Only modify the @media (min-width: 1920px) blocks
    - Do NOT delete any existing elements or CSS outside 1920px blocks
    - Do NOT change mobile styles
  </action>
  <verify>Screenshot at 1920x1080 shows no text overlap, logo opacity matches Figma reference</verify>
  <done>AC-2 and AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 4: Fix radio page button padding (sub-agent)</name>
  <files>app/pages/radio.vue</files>
  <action>
    Dispatch sub-agent to fix bottom padding for area buttons at 1920px.

    Current: .area-buttons has bottom: 4rem (base CSS). At 1920px the buttons are
    235x148px but no bottom override exists, so they clip the viewport edge.

    Fix: Add bottom offset override in the 1920px media query block.
    Check Figma MCP get_design_context on node 12:1185 for exact positioning.

    Rules:
    - Only add/modify the @media (min-width: 1920px) block
    - Do NOT change base or mobile styles
  </action>
  <verify>Screenshot at 1920x1080 shows buttons with visible bottom padding</verify>
  <done>AC-4 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>4 regression fixes + Figma reference screenshot library</what-built>
  <how-to-verify>
    1. Browse design-references/desktop-1920/ — confirm PNG for each page
    2. Run: npx nuxt dev
    3. Open browser at 1920x1080
    4. Click burger menu — verify "Home" is visible at top
    5. Visit / — check hero logo opacity matches Figma reference, eats+drinks heading doesn't overlap
    6. Visit /radio — check buttons have bottom padding
    7. Spot-check 2-3 other pages — verify no regressions
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any page files not listed in files_modified
- Mobile or tablet CSS on any page
- Content text or HTML structure
- Elements/features that were manually added and don't exist in Figma

## SCOPE LIMITS
- Only 1920px media query fixes
- Only the 4 identified bugs + reference screenshots
- No new features or layout changes beyond fixes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 11 Figma reference screenshots saved to design-references/desktop-1920/
- [ ] BurgerNav shows all 14 items at 1920x1080
- [ ] Home page hero logo opacity matches Figma
- [ ] Home page eats+drinks heading doesn't overlap subtitle
- [ ] Radio page buttons have visible bottom padding
- [ ] No regressions on other pages (spot check)
- [ ] Build passes: npx nuxt build
</verification>

<success_criteria>
- 4 bugs fixed with visual confirmation
- Reference screenshot library created
- No regressions introduced
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.paul/phases/03-content-pages/03-02-SUMMARY.md`
</output>
